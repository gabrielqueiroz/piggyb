<div class="col-md-4">
</div>
<div class="col-md-4">
  <div class="card mx-auto">
    <h5 class="text-center pt-3">sign up</h5>
    <div class="card-body">
      <%= form_with scope: :user, url: users_path do |form| %>
        <div class="form-group">
          <%= form.text_field :name, class: "form-control", placeholder: "name" %>
        </div>
        <div class="form-group">
          <%= form.text_field :email, class: "form-control", placeholder: "email" %>
        </div>
        <div class="form-group">
          <%= form.text_field :password, class: "form-control", placeholder: "password" %>
          <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>
        <div class="form-group">
          <%= form.text_field :confirm_password, class: "form-control", placeholder: "confirm password" %>
        </div>

        <%= form.submit "Sign up", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
<div class="col-md-4">
</div>
